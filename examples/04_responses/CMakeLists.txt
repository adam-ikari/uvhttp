# 响应处理示例

# 注释掉不存在的文件
# add_executable(static_files static_files.c)
# target_link_libraries(static_files ${UVHTTP_LIBRARY} uv pthread m)

# add_executable(websocket_server websocket_server.c)
# target_link_libraries(websocket_server ${UVHTTP_LIBRARY} uv pthread m)
